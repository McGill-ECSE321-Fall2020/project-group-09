<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.mcgill.ecse321.artgallerysystem">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 权限声明 -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtGallerySystemAndroid">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ArtGallerySystemAndroid.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".ArtPieceInfo" />
        <activity android:name=".Purchase"/>
        <activity android:name=".ApplyNewAddress"/>
        <activity android:name=".SuccessfulPayment"/>
        <activity android:name=".Login"/>
        <activity android:name=".Signup"/>
        <activity android:name=".CreateArtPiece"/>
        <activity android:name=".MyAccount" android:exported="true"/> <!-- exported for testing -->
        <activity android:name=".AccountPurchases" android:exported="true"/>
        <activity android:name=".AccountArtPieces" android:exported="true"/>
        <activity android:name=".AccountAddresses" android:exported="true"/>

    </application>

</manifest>